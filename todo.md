# To Do List

* hide cursor after 60 sec
* get server netBios name for final URL
* weather API
* can the news ticket be a slider rather than a marque so we have a loop?
  * width may be an issuer, if the copy is too long. Would need to truncater.